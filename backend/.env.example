# --- Live deals: choose one or both (feed is tried first, then OpenMenu) ---

# Optional: URL of a JSON feed. Your own API, a third-party, or same-server feed.
# Format: JSON array of deals, or { "deals": [...] }. Fields: title/headline, description,
# restaurant/restaurant_name, validUntil/date_end, cuisine, savings, location, image.
# DEALS_FEED_URL=

# Optional: OpenMenu API – no separate server. Get a key at https://www.openmenu.com/platform/account/api.php
# With only the key: app uses OpenMenu "sample" deals (no credits). With city+country: real local deals.
# OPENMENU_API_KEY=
# OPENMENU_CITY=
# OPENMENU_COUNTRY=US
# OPENMENU_STATE=
# OPENMENU_POSTAL_CODE=

# Optional: Scraper – comma-separated URLs to scan for deal-like content (see README).
# Only use sites you are allowed to scrape. Many restaurant sites prohibit scraping.
# SCRAPER_URLS=
# SCRAPER_INTERVAL_MS=3600000

# --- Server ---
# PORT=3001
